export const determineSeason = (): number => {
  // const today = new Date();
  // return today.getMonth() >= 6 ? today.getFullYear() : today.getFullYear() - 1;

  // until the COVID-19 pandemic is over, we are using historical data
  return 2018;
}